import {
  View,
  Text,
  TouchableOpacity,
  FlatList,
  SafeAreaView,
  ImageBackground,
  Modal,
  RefreshControl,
  ScrollView,
  Switch,
  TextInput,
  Image,
  TouchableWithoutFeedback,
  TouchableHighlight,
  KeyboardAvoidingView
} from 'react-native';
import { styled } from 'nativewind';
import FastImage from 'react-native-fast-image';

export const StyledText = styled(Text);
export const StyledTextInput = styled(TextInput);
export const StyledView = styled(View);
export const StyledTouchView = styled(TouchableOpacity);
export const StyledFlatList = styled(FlatList);
export const StyledSafeAreaView = styled(SafeAreaView);
export const StyledImage = styled(Image);
export const StyledImageBackground = styled(ImageBackground);
export const StyledModal = styled(Modal);
export const StyledRefreshControl = styled(RefreshControl);
export const StyledScrollView = styled(ScrollView);
export const StyledSwitch = styled(Switch);
export const StyledTouchableWithoutFeedback = styled(TouchableWithoutFeedback);
export const StyledTouchableHighlight = styled(TouchableHighlight);
export const StyledKeyboardAvoidingView = styled(KeyboardAvoidingView);
export const StyledFastImage = styled(FastImage);
